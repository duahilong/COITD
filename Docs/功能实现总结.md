# COITD 项目功能实现总结（截至当前）

## 1. 目录与边界调整

- 已将所有自定义脚本从 `CloudflareSpeedTest` 目录迁出。
- 当前自定义能力统一放在：`scripts/cfst/`
- `CloudflareSpeedTest` 保持原始项目状态，不再承载 COITD 定制脚本。

## 2. 已实现功能

### 2.1 配置文件驱动启动 CFST

- 启动器文件：`scripts/cfst/cfst_config_runner.py`
- 支持通过配置文件组装并执行 CFST 命令。
- 支持从结果 CSV 自动提取优选 IP。

### 2.2 配置格式与参数能力

- 支持 `JSON` 与 `JSONC`（允许 `//`、`/* */` 注释）。
- 兼容带 BOM 的配置文件读取（`utf-8-sig`）。
- 支持 `cfst -h` 全量参数映射（`options` 节点）。
- 支持参数名严格校验（`strict_known_options`）。
- 支持参数别名：`httping_code` -> `httping-code`。

### 2.3 优选 IP 输出能力

- 支持通过 `best_ip_count` 控制输出数量（Top N）。
- 标准输出：
  - `BEST_IP=...`（第一名）
  - `BEST_IP_LIST=...`（当 `best_ip_count > 1`）
- 文件输出：
  - `best_ip_file` 写入前 N 个 IP（每行一个）。

### 2.4 日本地区机房优选模板

- 模板文件：`scripts/cfst/cfst_config.jp.jsonc`
- 已预置 `HTTPing + cfcolo 日本地区码` 过滤。
- 可通过 `options.f` 指向“特定 IP 文件”。

### 2.5 其他脚本

- 两阶段流水线脚本（Windows）：`scripts/cfst/cfst_pipeline.ps1`
- 使用说明：`scripts/cfst/cfst_pipeline.md`
- 配置说明：`scripts/cfst/cfst_config_runner.md`

## 3. 关键配置项（新增/重点）

- `best_ip_count`：输出并写入前 N 个优选 IP。
- `best_ip_file`：保存优选 IP 结果文件。
- `options.f`：指定输入 IP 文件（特定 IP 文件）。
- `options.httping` + `options.cfcolo`：地区机房过滤关键组合。

## 4. 实测验证结论

测试环境：`10.0.0.18`（Linux）  
测试方式：使用 `scripts/cfst/cfst_config_runner.py` 执行配置文件

- 配置驱动执行：通过
- 指定输入文件 `ip.txt`：通过
- 日本机房过滤（NRT/HND/KIX...）：通过
- Top N 输出（`best_ip_count=3`）：通过
- `best_ip_file` 多行写入：通过

## 5. 当前文件清单（COITD 自定义）

- `scripts/cfst/cfst_config_runner.py`
- `scripts/cfst/cfst_config_runner.md`
- `scripts/cfst/cfst_config.full.json`
- `scripts/cfst/cfst_config.jp.jsonc`
- `scripts/cfst/cfst_config.json`
- `scripts/cfst/cfst_pipeline.ps1`
- `scripts/cfst/cfst_pipeline.md`

## 6. 最新测试进度（2026-04-18）

### 6.1 配置调整记录

- 用户对 `scripts/cfst/cfst_config.jp.jsonc` 做了多轮调参验证。
- 关键参数调整方向：
  - 并发从 `n=200` 调整为 `n=100`
  - 延迟上限从 `tl=90` 调整为 `tl=100`
  - 下载测速时长设置为 `dt=20`
  - 丢包率上限使用 `tlr=0.5`
  - 日本地区码：`NRT,HND,KIX,NGO,FUK,OKA`

### 6.2 最新一轮测试结果

测试环境：`10.0.0.18`（Linux）  
执行配置：`scripts/cfst/cfst_config.jp.jsonc`

- 执行状态：通过（退出码 `0`）
- 本轮优选结果：`BEST_IP=172.64.229.131`
- 结果文件：`/root/coitd/result_jp.csv`
- 最优 IP 文件：`/root/coitd/best_ip_jp.txt`

结果样例：

- `172.64.229.131,4,4,0.00,96.04,38.16,NRT`

### 6.3 现状结论

- 当前“特定 IP 文件 + 日本地区过滤 + 配置文件驱动”的流程已跑通。
- 在较严格筛选参数下，可用 IP 数量可能较少（本轮仅 1 条进入下载测速）。
- 启动器已具备稳定输出 `BEST_IP` 的能力，可直接用于下一步自动化流程对接。
