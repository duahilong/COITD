## 基础信息
### 项目名称：COITD
1. 测试服务器（所有测试都在该服务器进行）
   - IP：`10.0.0.18`
   - 用户：`root`
   - 密码：`duhailong`
2. 尽量使用 `python3` 实现外围代码和脚本部分。
3. 所有代码都必须在测试服务器上运行，不能在本地开发环境运行。
4. 项目主要运行在 Linux 服务器上。
5. `CloudflareSpeedTest` 目录为只读目录：禁止修改、写入、删除；仅允许读取。
